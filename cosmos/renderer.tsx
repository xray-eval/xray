import { mountDomRenderer } from "react-cosmos-dom";

// Generated by `cosmos --expose-imports` (see cosmos.config.json) and
// gitignored: it names every fixture file on disk, so committing it would put
// a machine-written file in the path of every branch that adds a fixture.
import * as mountArgs from "./cosmos.imports.ts";

mountDomRenderer(mountArgs);
